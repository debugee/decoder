{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/install/bin/bochs",
        "args": [],
        "cwd": "${workspaceFolder}",
        "console":"integratedTerminal"
    }
    ]
}